# Docksmith sample application
# Uses all 6 required instructions: FROM, COPY, RUN, WORKDIR, ENV, CMD

FROM alpine:3.18

WORKDIR /app

ENV GREETING=Hello
ENV APP_VERSION=1.0.0

COPY . /app

RUN sh -c "echo hello > /newfile.txt"

CMD ["sh", "/app/run.sh"]
